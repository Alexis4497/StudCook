{% extends 'base.html.twig' %}

{% block title %}Nos Recettes{% endblock %}

 {% block body %}

         <div>
            <h2>Les dernières recettes</h2>
                <div>
                    {% for r in recettes %}

                        <div class="video">

                        <iframe width="40%" height="100%" src={{r.lienVideo}}></iframe>

                        

                        </div>

                        
                            <div>
                                <h2>{{r.titreRecette}}</h2>
                                
                                <p> {{r.descriptionRecette}} </p>
                                <h3> {{r.prixRecette}} € </h3>

                            </div>
                    
                    {% endfor %}
                </div>
        </div>


      
{% endblock %}

