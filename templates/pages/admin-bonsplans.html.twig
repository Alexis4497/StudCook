{% extends 'base.html.twig' %}

{% block title %}Admin Bons Plans{% endblock %}

 {% block body %}
 
    <div class="container mt-4">

        <h1>Gérer les Bons Plans</h1>

        {% for message in app.flashes('success') %}

            <div class="alert alert-success">
                {{ message }}
            </div>
            
        {% endfor %}
        
        <div class="text-right">
            <a href="{{ path('adminbonsplanscreate') }}" class="btn btn-primary">Ajouter un nouveau Bon Plan</a>

        </div>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Titre</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
             {% for b in adminbonsplans %}
                <tr>
                    <td>{{ b.nomBonPlan }}</td>
                    <td>
                        <a href="{{ path('adminbonsplansedit', {id: b.id}) }}" class="btn btn-secondary">Editer</a>
                        <form method="post" action="{{ path('deletebonsplans', {id: b.id}) }}" style="display:inline-block" 
                            onsubmit="return confirm('Êtes vous vraiment sûr de vouloir supprimer ce bon plan ?')">
                            
                                <input type="hidden" name="_method" value="DELETE">
                                <button class="btn btn-danger"> Supprimer </button>
                            </form>
                    </td>
                </tr>
              
            {% endfor %}
            </tbody>
        </table>

        

     
               
      
{% endblock %}

